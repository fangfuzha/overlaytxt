# 弹幕背景蒙版产生分割线

**现象**：`display_area_percent: 0.5` 时，上半屏有半透明蒙版，且上下半屏之间有可见分割线。

**原因**：Clear(alpha=0) 的全透明区域与半透明背景矩形（alpha=0.3）之间 alpha 突变，形成视觉分割线。

**解决**：背景层作为可选功能（`background_enabled`，默认关闭），由用户选择是否开启。Clear 始终全透明，仅在背景开启时绘制半透明矩形。